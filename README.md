# 📝 Infotecs-Test-Task

## 🚀 **Быстрый старт**

```bash
# 1. Клонировать проект
git clone https://github.com/ArtemW2/INFOTECS.git
cd INFOTECS

# 2. Установить зависимости (Если uv не установлен, предварительно выполнить команду pip install uv)
uv sync

# 3. Активировать виртуальное окружение
.venv/scripts/activate

# 4. Запустить сервер
python main.py

# 5. Открыть документацию
http://127.0.0.1:8000/docs


```
# 📌 Получение всех доступных городов


## 🔗 Endpoint
`GET /cities/`

## 📝 Описание
Список всех городов, хранящихся в собственной БД, для которых хранятся данные о погоде с актуальностью 15 минут.

---

## 📥 Пример ответа

```http
[
  {
    "id": 1,
    "name": "Томск",
    "latitude": 55,
    "longitude": 86
  },
  {
    "id": 2,
    "name": "Москва",
    "latitude": 55,
    "longitude": 36
  },
  {
    "id": 3,
    "name": "Санкт-Петербург",
    "latitude": 59,
    "longitude": 30
  }
]
```

# 📌 Добавление нового города

## 🔗 Endpoint
`POST /cities/`

## 📝 Описание
Город добавляется в БД и добавляется к списку городов, получающих актуальные данные о погоде каждые 15 минут. Сразу после добавления будут доступны данные о погоде для этого города. Если город уже есть в БД, вернутся данные о существующей записи, данные запроса внесены в БД не будут (Проверка корректности координат для города не предусмотрена).

---

## 🧩 Параметры запроса

| Поле              | Тип       | Обязательное  | Описание         |
|-------------------|-----------|---------------|------------------|
| `name`            | string    | ✔️           | Название города  |
| `latitude`        | string    | ✔️           | Широта           |
| `latitude`        | string    | ✔️           | Долгота          |


---

## 📤 Пример запроса

```http

{
  "name": "Томск",
  "latitude": 55
  "longitude": 86
}
```

## 📥 Пример ответа

```http
{
  "id": 1
  "name": "Томск",
  "latitude": 55
  "longitude": 86
}
```

# 📌 Удаление города из отслеживаемых

## 🔗 Endpoint
`DELETE /cities/{name}`


## 📝 Описание
Сервис больше не станет отслеживать данные о погоде для этого города. Все новые данные придётся получать либо по координатам(GET /weather/current), либо добавлять город в БД вновь

---

## 📥 Пример ответа

```http
{
  "message": "Город успешно удалён"
}
```
---


# 📌 Получених данные о погоде по координатам на момент запроса

## 🔗 Endpoint
`GET /weather/current/`

## 📝 Описание
Возвращает данные о погоде по введённым пользователем координатам. Город, находящийся по этим координатам в БД не вносится, данные не сохраняются!


---

## 🧩 Параметры запроса

| Поле              | Тип       | Обязательное | Описание          |
|-------------------|-----------|--------------|-------------------|
| `latitude`        | string    | ✔️           | Широта           |
| `latitude`        | string    | ✔️           | Долгота          |

---

---

## 📤 Пример запроса

```http
{
  "latitude": 55,
  "longitude": 86
}
```

## 📥 Пример ответа

```http
{
  "temperature": -8.6,
  "wind_speed": 12.4,
  "pressure_msl": 1032.5
}
```


# 📌 Получение данных о погоде(доступны фильтры) по названию города и времени суток(час)

## 🔗 Endpoint
`GET /weather/`

## 📝 Описание
Пользователь вводит название города и время суток в часах(0-23).
Сперва сервис ищет данные о городе в своей БД, если город найден, то данные будут взяты из собственной БД.
Если город не обнаружен в БД, сервер отправляет запрос к API, получает координаты, получает данные о погоде по координатам и сохраняет новый город в своей БД вместе с данными о погоде. 
После этого город будет добавлен в периодическую задачу по отслеживанию погоды каждые 15 минут.

Доступны фильтры: температура, атмосферное давление, скорость ветра, осадки, влажность.
Если ни один фильтр не будет выбран, в ответе вернутся данные, аналогичные эндпоинту GET /weather/current/ в час, указанный пользователем.

---

## 🧩 Параметры запроса

| Поле          | Тип          | Обязательное | Описание                |
|---------------|--------------|--------------|-------------------------|
| `name`        | string       | ✔️           | Название города        |
| `hour`        | int          | ✔️           | Час суток              |
| `filters`     | List[string] |  X            | Фильтры данных о погоде|

---

---

## 📤 Пример запроса

```http

{
   "name": "Томск",
   "hour": 22,
   "filters": "temperature", "precipitation"
}
```

## 📥 Пример ответа

```http
{
  "temperature": -10.3,
  "wind_speed": null,
  "pressure_msl": null,
  "humidity": null,
  "precipitation": 0
}
```



